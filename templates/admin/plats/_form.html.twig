{{ form_start(platForm)}}
    {{ form_row(platForm.libelle)}}
    {{ form_row(platForm.description)}}
    {{ form_row(platForm.categorie)}}
    {{ form_row(platForm.prix)}}
    {{ form_row(platForm.active)}}

    {# on affiche les images si on est en édition 
    {% if app.request.attributes.get('_route') == 'admin_plat_edit' %}
        <h2>Images</h2>
        {% for image in plat.image %}
            <div>
                <img src="{{ asset('assets/images/images_the_district/plats/' )}}" alt="Image" width="150">
                <a href="" data-delete data-token="{{ csrf_token('delete' ~ plat.image)}}">Supprimer</a>
            </div>
        {% endfor %}
    {% endif %} #}
    
    {{ form_row(platForm.image)}}

    <button type="submit" class="btn btn-primary">{{ button_label|default('Ajouter') }}</button>
{{ form_end(platForm)}}
